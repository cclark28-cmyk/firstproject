# firstproject

Group A: Functions + Array Methods - Ariel, Imogen, Ananaya, Beckett
Your program must make meaningful use of:

Functions — break your program into logical pieces. You should have multiple functions, each doing one clear job (not one giant function that does everything).
.map() — use it to transform a list of data into a new list.
.filter() — use it to narrow down a list based on some condition.
